<%= form_with(model: tarot_answer, local: true) do |form| %>
  <% if tarot_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tarot_answer.errors.count, "error") %> prohibited this tarot_answer from being saved:</h2>

      <ul>
      <% tarot_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :tarot_answer_content %>
  </div>

  <div class="field">
    <%= form.label :next_bot_id %>
    <%= form.number_field :next_bot_id, id: :tarot_answer_next_bot_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
