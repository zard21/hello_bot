<%= form_with(model: tarot_bot, local: true) do |form| %>
  <% if tarot_bot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tarot_bot.errors.count, "error") %> prohibited this tarot_bot from being saved:</h2>

      <ul>
      <% tarot_bot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :tarot_bot_content %>
  </div>

  <div class="field">
    <%= form.label :ans_type %>
    <%= form.text_field :ans_type, id: :tarot_bot_ans_type %>
  </div>

  <div class="field">
    <%= form.label :is_init %>
    <%= form.check_box :is_init, id: :tarot_bot_is_init %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
